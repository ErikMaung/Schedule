/*
function restoreAndSkipContent() {
    var hidden = document.getElementsByClassName('jump')[0];

    hidden.classList.add('jump2');
    window.scroll(0, hidden.offsetHeight);
  };
  restoreAndSkipContent();
*/